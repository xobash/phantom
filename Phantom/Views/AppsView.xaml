<UserControl x:Class="Phantom.Views.AppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <UserControl.Resources>
        <Style x:Key="AppsActionButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Content" Value="⋯"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource DarkCardStyle}">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="Installed Apps" FontSize="22" FontWeight="SemiBold" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBox DockPanel.Dock="Left" Width="300" Margin="0,0,12,0" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}" />
                <Button DockPanel.Dock="Left" Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource DarkButtonStyle}" />
                <TextBlock DockPanel.Dock="Right" Text="{Binding AppsCountLabel}" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}" FontSize="14" Margin="12,0,0,0"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource DarkCardStyle}">
            <DataGrid ItemsSource="{Binding AppsView}" AutoGenerateColumns="False" IsReadOnly="True"
                      Style="{StaticResource DarkDataGridStyle}"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True" EnableRowVirtualization="True">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="App" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Foreground="{DynamicResource MutedTextBrush}" FontSize="13">
                                        <Run Text="{Binding Version}"/>
                                        <Run Text=" | "/>
                                        <Run Text="{Binding Publisher}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Size" Binding="{Binding SizeOnDisk}" Width="120"/>
                    <DataGridTextColumn Header="Install date" Binding="{Binding InstallDate}" Width="140"/>

                    <DataGridTemplateColumn Header="" Width="52">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource AppsActionButtonStyle}" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}" Click="OpenRowMenu">
                                    <Button.ContextMenu>
                                        <ContextMenu Style="{StaticResource DarkContextMenuStyle}" ItemContainerStyle="{StaticResource DarkContextMenuItemStyle}">
                                            <ContextMenu.Resources>
                                                <Style TargetType="Separator" BasedOn="{StaticResource DarkContextMenuSeparatorStyle}"/>
                                            </ContextMenu.Resources>
                                            <MenuItem Header="Browse folder"
                                                      Command="{Binding PlacementTarget.Tag.BrowseCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Search online"
                                                      Command="{Binding PlacementTarget.Tag.SearchOnlineCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <Separator/>
                                            <MenuItem Header="Uninstall"
                                                      Command="{Binding PlacementTarget.Tag.UninstallCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
