<UserControl x:Class="Phantom.Views.AutomationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource DarkCardStyle}">
            <WrapPanel>
                <Button Content="Export config" Command="{Binding ExportConfigCommand}" Style="{StaticResource DarkButtonStyle}"/>
                <Button Content="Import config" Command="{Binding ImportConfigCommand}" Style="{StaticResource DarkButtonStyle}"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource DarkCardStyle}">
            <TextBox Text="{Binding PreviewText}" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Style="{StaticResource DarkTextBoxStyle}"/>
        </Border>
    </Grid>
</UserControl>
