<UserControl x:Class="Phantom.Views.LogsAboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource DarkCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="2.5*"/>
                    <RowDefinition Height="1.4*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock DockPanel.Dock="Top" Text="About (README excerpt)" Margin="0,0,0,8" FontWeight="SemiBold" FontSize="18"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBox Grid.Row="0"
                                 Margin="0,0,0,8"
                                 Text="{Binding AboutBanner}"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Disabled"
                                 HorizontalScrollBarVisibility="Auto"
                                 TextWrapping="NoWrap"
                                 FontFamily="Consolas"
                                 FontSize="8.5"
                                 Foreground="#B95AFF"
                                 Background="{DynamicResource TerminalBackgroundBrush}"
                                 BorderBrush="{DynamicResource DarkBorderBrush}"
                                 BorderThickness="1"
                                 Padding="8,6"/>
                        <TextBox Grid.Row="1"
                                 Text="{Binding AboutText}"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 TextWrapping="Wrap"
                                 FontFamily="Consolas"
                                 FontSize="15"
                                 Style="{StaticResource DarkTextBoxStyle}"/>
                    </Grid>
                </DockPanel>

                <DockPanel Grid.Row="1">
                    <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                        <Button Content="Refresh logs" Command="{Binding RefreshLogsCommand}" Style="{StaticResource DarkButtonStyle}"/>
                        <Button Content="Open" Command="{Binding OpenLogCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    </WrapPanel>
                    <ListBox ItemsSource="{Binding LogFiles}" SelectedItem="{Binding SelectedLog}" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource DarkTextBrush}" BorderBrush="{DynamicResource DarkBorderBrush}" BorderThickness="1"/>
                </DockPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource DarkCardStyle}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Log content" Margin="0,0,0,8" FontWeight="SemiBold" FontSize="18" Foreground="{DynamicResource DarkTextBrush}"/>
                <TextBox Text="{Binding SelectedLogContent}" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap" FontFamily="Consolas" Style="{StaticResource DarkTextBoxStyle}"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
