<UserControl x:Class="Phantom.Views.LogsAboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource DarkCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="About (README excerpt)" FontWeight="SemiBold" FontSize="18" Foreground="{DynamicResource DarkTextBrush}" Margin="0,0,0,8"/>
                    <Viewbox Stretch="Uniform" StretchDirection="DownOnly" Height="92" Margin="0,0,0,10">
                        <TextBlock Text="{Binding AboutLogo}"
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="#A55DFF"
                                   TextWrapping="NoWrap"/>
                    </Viewbox>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300" Margin="0,0,0,10">
                        <TextBlock Text="{Binding AboutText}" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}" />
                    </ScrollViewer>
                </StackPanel>

                <DockPanel Grid.Row="1">
                    <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                        <Button Command="{Binding RefreshLogsCommand}"
                                Style="{StaticResource RefreshIconButtonStyle}"
                                ToolTip="Refresh log file list"/>
                        <Button Content="Open logs folder" Command="{Binding OpenLogCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    </WrapPanel>
                    <ListBox ItemsSource="{Binding LogFiles}"
                             SelectedItem="{Binding SelectedLog}"
                             Background="{DynamicResource InputBackgroundBrush}"
                             Foreground="{DynamicResource DarkTextBrush}"
                             BorderBrush="{DynamicResource DarkBorderBrush}"
                             BorderThickness="1"/>
                </DockPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource DarkCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Log content" FontWeight="SemiBold" FontSize="18" Foreground="{DynamicResource DarkTextBrush}" Margin="0,0,0,8"/>
                <TextBox Grid.Row="1"
                         Text="{Binding SelectedLogContent}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas"
                         Style="{StaticResource DarkTextBoxStyle}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
