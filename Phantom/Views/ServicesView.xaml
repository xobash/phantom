<UserControl x:Class="Phantom.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <UserControl.Resources>
        <Style x:Key="ServicesActionButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Content" Value="⋯"/>
        </Style>
        <Style x:Key="InfoChipStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="{DynamicResource DarkCard2Brush}"/>
        </Style>
        <Style x:Key="DarkMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource DarkTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DarkCard2Brush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,6"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowHoverBackgroundBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource DarkCardStyle}">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="Services" FontSize="22" FontWeight="SemiBold" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBox DockPanel.Dock="Left" Width="300" Margin="0,0,12,0" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}" />
                <Button DockPanel.Dock="Left" Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource DarkButtonStyle}" />
                <TextBlock DockPanel.Dock="Right" Text="{Binding ServicesCountLabel}" VerticalAlignment="Center" Foreground="{DynamicResource MutedTextBrush}" FontSize="14" Margin="12,0,0,0"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource DarkCardStyle}">
            <DataGrid ItemsSource="{Binding ServicesView}" AutoGenerateColumns="False" IsReadOnly="True"
                      Style="{StaticResource DarkDataGridStyle}"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True" EnableRowVirtualization="True">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Service" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Foreground="{DynamicResource MutedTextBrush}" FontSize="13">
                                        <Run Text="{Binding Name}"/>
                                        <Run Text=" | "/>
                                        <Run Text="{Binding PathName}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130"/>
                    <DataGridTextColumn Header="Mode" Binding="{Binding StartupType}" Width="130"/>

                    <DataGridTemplateColumn Header="" Width="42">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource InfoChipStyle}" Cursor="Help"
                                        ToolTipService.ShowDuration="60000"
                                        ToolTipService.InitialShowDelay="120">
                                    <Border.ToolTip>
                                        <ToolTip Background="{DynamicResource DarkCard2Brush}"
                                                 Foreground="{DynamicResource DarkTextBrush}"
                                                 BorderBrush="{DynamicResource DarkBorderBrush}"
                                                 BorderThickness="1"
                                                 MaxWidth="520"
                                                 Placement="Mouse">
                                            <TextBlock Text="{Binding Summary}"
                                                       TextWrapping="Wrap"
                                                       Foreground="{DynamicResource DarkTextBrush}"/>
                                        </ToolTip>
                                    </Border.ToolTip>
                                    <TextBlock Text="i" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="" Width="52">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource ServicesActionButtonStyle}" Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}" Click="OpenRowMenu">
                                    <Button.ContextMenu>
                                        <ContextMenu Background="{DynamicResource DarkCard2Brush}" BorderBrush="{DynamicResource DarkBorderBrush}" BorderThickness="1">
                                            <ContextMenu.Resources>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource DarkMenuItemStyle}"/>
                                            </ContextMenu.Resources>
                                            <MenuItem Header="Stop"
                                                      Command="{Binding PlacementTarget.Tag.StopServiceCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Restart"
                                                      Command="{Binding PlacementTarget.Tag.RestartServiceCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Browse"
                                                      Command="{Binding PlacementTarget.Tag.BrowseServiceLocationCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <MenuItem Header="Search"
                                                      Command="{Binding PlacementTarget.Tag.SearchOnlineCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            <Separator/>
                                            <MenuItem Header="Mode">
                                                <MenuItem Header="Automatic"
                                                          Command="{Binding PlacementTarget.Tag.SetAutomaticModeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Manual"
                                                          Command="{Binding PlacementTarget.Tag.SetManualModeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                <MenuItem Header="Disabled"
                                                          Command="{Binding PlacementTarget.Tag.SetDisabledModeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                          CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                            </MenuItem>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
