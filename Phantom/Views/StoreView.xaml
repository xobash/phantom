<UserControl x:Class="Phantom.Views.StoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <UserControl.Resources>
        <Style x:Key="StoreGridTextStyle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{DynamicResource DarkTextBrush}"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource DarkCardStyle}" Margin="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,8">
                    <TextBlock Text="Package Managers"
                               FontSize="21"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource DarkTextBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="winget: " FontSize="18" Foreground="{DynamicResource MutedTextBrush}"/>
                        <TextBlock Text="{Binding WingetInstalled}" FontSize="18" FontWeight="SemiBold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#FF5C5C"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WingetInstalled}" Value="True">
                                            <Setter Property="Foreground" Value="#5BD671"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="choco: " FontSize="18" Foreground="{DynamicResource MutedTextBrush}"/>
                        <TextBlock Text="{Binding ChocoInstalled}" FontSize="18" FontWeight="SemiBold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#FF5C5C"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ChocoInstalled}" Value="True">
                                            <Setter Property="Foreground" Value="#5BD671"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,0,8">
                    <Button Command="{Binding RefreshManagersCommand}"
                            Style="{StaticResource RefreshIconButtonStyle}"
                            ToolTip="Refresh package manager status"/>
                    <Button Content="Install missing managers" Command="{Binding InstallMissingManagersCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Install winget" Command="{Binding InstallWingetCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Uninstall winget" Command="{Binding UninstallWingetCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Install choco" Command="{Binding InstallChocoCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Uninstall choco" Command="{Binding UninstallChocoCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Import catalog" Command="{Binding ImportCatalogCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Export catalog" Command="{Binding ExportCatalogCommand}" Style="{StaticResource DarkButtonStyle}"/>
                </StackPanel>

                <TextBox Grid.Row="2"
                         Height="38"
                         Margin="0,0,0,8"
                         VerticalContentAlignment="Center"
                         Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource DarkTextBoxStyle}"/>

                <StackPanel Grid.Row="3" Margin="0,0,0,8">
                    <Button Content="Install selected" Command="{Binding InstallSelectedCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Uninstall selected" Command="{Binding UninstallSelectedCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Upgrade selected" Command="{Binding UpgradeSelectedCommand}" Style="{StaticResource DarkButtonStyle}"/>
                </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>

        <Border Grid.Column="2" Style="{StaticResource DarkCardStyle}" Margin="0">
            <DataGrid ItemsSource="{Binding CatalogView}"
                      AutoGenerateColumns="False"
                      Style="{StaticResource DarkDataGridStyle}"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True"
                      EnableRowVirtualization="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Sel"
                                            Binding="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="64"/>
                    <DataGridTemplateColumn Header="App" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}"
                                           Style="{StaticResource StoreGridTextStyle}"
                                           ToolTip="{Binding DisplayName}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
