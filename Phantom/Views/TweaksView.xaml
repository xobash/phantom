<UserControl x:Class="Phantom.Views.TweaksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource DarkCardStyle}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Width="300">
                    <TextBlock Text="Tweaks" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource DarkTextBrush}"/>
                    <TextBox Margin="0,10,0,0" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DarkTextBoxStyle}"/>
                </StackPanel>

                <WrapPanel Margin="12,0,0,0" VerticalAlignment="Top">
                    <Button Content="Refresh status" Command="{Binding RefreshStatusCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Apply selected" Command="{Binding ApplySelectedCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Undo selected" Command="{Binding UndoSelectedCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Preset: Basic" Command="{Binding ApplyBasicPresetCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Preset: Advanced" Command="{Binding ApplyAdvancedPresetCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Clear" Command="{Binding ClearSelectionCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Import selection" Command="{Binding ImportSelectionCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <Button Content="Export selection" Command="{Binding ExportSelectionCommand}" Style="{StaticResource DarkButtonStyle}"/>
                    <CheckBox Content="Dry-run" Margin="8,12,0,0" IsChecked="{Binding DryRun}" Style="{StaticResource DarkCheckBoxStyle}"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource DarkCardStyle}">
            <DataGrid ItemsSource="{Binding TweaksView}" AutoGenerateColumns="False"
                      Style="{StaticResource DarkDataGridStyle}"
                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True" EnableRowVirtualization="True">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Sel" Binding="{Binding Selected, Mode=TwoWay}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="280"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Risk" Binding="{Binding RiskTier}" Width="90"/>
                    <DataGridTextColumn Header="Scope" Binding="{Binding Scope}" Width="80"/>
                    <DataGridCheckBoxColumn Header="Rev" Binding="{Binding Reversible}" Width="52"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="2" Margin="10,2,10,0" Text="Dangerous or non-reversible tweaks require explicit confirmation (Y)." Foreground="{DynamicResource MutedTextBrush}"/>
    </Grid>
</UserControl>
