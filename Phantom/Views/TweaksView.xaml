<UserControl x:Class="Phantom.Views.TweaksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource AppBgBrush}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="FeatureRowBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource DarkCardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="6,4"/>
            <Setter Property="Padding" Value="14,10"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RowHoverBackgroundBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBorderBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FeatureTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource DarkTextBrush}"/>
        </Style>

        <Style x:Key="FeatureDescriptionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
        </Style>

        <Style x:Key="FlatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource DarkCardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="6,4"/>
        </Style>

        <Style x:Key="CompactButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="MinWidth" Value="72"/>
        </Style>

        <Style x:Key="ChevronToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Content" Value="▸"/>
            <Setter Property="Foreground" Value="{DynamicResource DarkTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource DarkCard2Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DarkBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ChevronBorder"
                                CornerRadius="7"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ChevronBorder" Property="Background" Value="{DynamicResource RowHoverBackgroundBrush}"/>
                                <Setter TargetName="ChevronBorder" Property="BorderBrush" Value="{DynamicResource AccentBorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ChevronBorder" Property="Background" Value="{DynamicResource NavItemSelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ChevronBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Content" Value="▾"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Background="{DynamicResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource FlatCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="340"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Tweaks" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource DarkTextBrush}"/>
                    <TextBlock Margin="0,4,0,0"
                               Foreground="{DynamicResource MutedTextBrush}"
                               Text="Use grouped sections. Persistent tweaks use switches; one-shot tweaks use action buttons."/>
                </StackPanel>

                <TextBox Grid.Column="1"
                         VerticalAlignment="Center"
                         Margin="0,0,10,0"
                         Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource DarkTextBoxStyle}"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Command="{Binding RefreshStatusCommand}"
                            Style="{StaticResource RefreshIconButtonStyle}"
                            ToolTip="Refresh tweak status"/>
                    <StackPanel Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                        <CheckBox IsChecked="{Binding ExpandAllSections}" Style="{StaticResource ToggleSwitchCheckBoxStyle}"/>
                        <TextBlock Text="Expand all" Margin="8,2,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Background="{DynamicResource AppBgBrush}">
            <StackPanel Background="{DynamicResource AppBgBrush}" Margin="0,2,0,6">
                <ItemsControl ItemsSource="{Binding Sections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <Border Style="{StaticResource FeatureRowBorderStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Title}" Style="{StaticResource FeatureTitleStyle}"/>
                                            <TextBlock Text="{Binding Description}" Style="{StaticResource FeatureDescriptionStyle}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1"
                                                   VerticalAlignment="Center"
                                                   Margin="10,0,12,0"
                                                   Foreground="{DynamicResource MutedTextBrush}"
                                                   FontSize="16"
                                                   Text="{Binding Tweaks.Count, StringFormat={}{0}}"/>
                                        <ToggleButton Grid.Column="2"
                                                      VerticalAlignment="Center"
                                                      IsChecked="{Binding IsExpanded, Mode=TwoWay}"
                                                      Style="{StaticResource ChevronToggleStyle}"/>
                                    </Grid>
                                </Border>

                                <Border Style="{StaticResource FeatureRowBorderStyle}"
                                        Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ItemsControl ItemsSource="{Binding Tweaks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,3">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                                        <TextBlock Text="{Binding Name}" FontSize="17" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding Description}"
                                                                   Margin="0,2,0,0"
                                                                   Style="{StaticResource FeatureDescriptionStyle}"
                                                                   TextWrapping="Wrap"/>
                                                    </StackPanel>

                                                    <TextBlock Grid.Column="1"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,16,0"
                                                               Text="{Binding Status}"
                                                               Style="{StaticResource FeatureDescriptionStyle}"/>

                                                    <Grid Grid.Column="2"
                                                          VerticalAlignment="Center">
                                                        <CheckBox VerticalAlignment="Center"
                                                                  IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                  Click="OnTweakToggleClick">
                                                            <CheckBox.Style>
                                                                <Style TargetType="CheckBox" BasedOn="{StaticResource ToggleSwitchCheckBoxStyle}">
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsActionButton}" Value="True">
                                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </CheckBox.Style>
                                                        </CheckBox>

                                                        <Button VerticalAlignment="Center"
                                                                MinWidth="84"
                                                                Content="{Binding ActionButtonText}"
                                                                Click="OnTweakActionClick">
                                                            <Button.Style>
                                                                <Style TargetType="Button" BasedOn="{StaticResource CompactButtonStyle}">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsActionButton}" Value="True">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </Grid>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Margin="10,2,10,0">
            <TextBlock Foreground="{DynamicResource MutedTextBrush}"
                       Text="Dangerous or non-reversible tweaks still require explicit confirmation (Y)."/>
            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <Button Content="Run O&amp;O ShutUp10"
                        Command="{Binding RunOoShutUp10Command}"
                        Style="{StaticResource DarkButtonStyle}"/>
                <StackPanel Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center"
                               Margin="0,0,8,0"
                               Foreground="{DynamicResource AccentBrush}"
                               FontWeight="SemiBold"
                               Text="DNS"/>
                    <ComboBox Width="280"
                              ItemsSource="{Binding DnsProfiles}"
                              SelectedItem="{Binding SelectedDnsProfile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              Style="{StaticResource DarkComboBoxStyle}"/>
                    <Button Content="Apply DNS"
                            Margin="8,0,0,0"
                            Command="{Binding ApplyDnsProfileCommand}"
                            Style="{StaticResource DarkButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
